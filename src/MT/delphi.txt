unit Unit1;

interface         // ќпредел€ет внешнее представление этого модул€

Uses
  Forms;

type
  TForm1 = class(TForm)
    procedure FormCreate(Sender: TObject);
  end;

var
  Form1: TForm1;

Implementation   
{$R *.dfm}      

Uses
  Dialogs, SysUtils;

procedure SayNumber(number : Integer);
begin
  ShowMessage('Number = '+SysUtils.IntToStr(number));
end;

procedure TForm1.FormCreate(Sender: TObject);
begin
  SayNumber(123);
end;

end.