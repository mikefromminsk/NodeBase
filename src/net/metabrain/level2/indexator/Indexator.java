package net.metabrain.level2.indexator;

import com.google.gson.JsonObject;
import net.metabrain.level2.consolidator.Consolidator;

public class Indexator {

    static void recursiveIndexation(JsonObject eventObject, String path) {

        //for  in (eventObject)
        //recursivePermutationPut(eventObject.get(key), path + "/" + key)


    }

    public static void index(JsonObject eventObject, String path) {

    }
}
